# ActionShot 要件定義書

## 1. プロジェクト概要

**「視覚情報（スクリーンショット）から、”ちまちま入力”を根絶し、”次のアクション”へ直通する架け橋」**

ユーザーがスマホで撮影・保存したアナログな情報の  
「デジタル化」から「外部アプリでの実行」までの摩擦をゼロにすることを目的とした、  
**超軽量・高機能なアクション・ハブ**。

---

## 2. コア・コンセプト

### 推論より構造化
- AIに未来の計画を立てさせるのではなく  
  **画像内の事実をデジタルデータに整理することに徹する**

### シンプルイズベスト
- 過剰なプロンプトを避け  
  **AIの素の推論能力を最大限に活かす**

### ユーザー主導
- AIの下書きをユーザーがその場で修正し  
  **完璧なデータでアクションを実行する**

---

## 3. カテゴリ判定と動作定義

AIは画像を解析し、以下の3カテゴリのいずれかに分類して  
UIとアクションを最適化する。

| カテゴリ | 判定基準 | 主要アクション | UI表示項目 |
|---|---|---|---|
| **EVENT** | 具体的な「日時」と「場所」が明記されている | カレンダー、マップ、LINE共有 | 全項目（日時、場所含む） |
| **PLACE** | 飲食店・施設など「場所」が主役、日時は不明 | マップ検索、Web検索、LINE共有 | 名称、場所、詳細のみ |
| **PRODUCT** | 物体（商品・食べ物など）が主役 | 画像検索、Web検索、LINE共有 | 名称、詳細のみ |

---

## 4. 機能要件

### 4.1 AI解析ロジック

**抽出データ**
- 名称
- 日時（開始・終了）
- 場所
- 詳細
- 関連URL

**データ補完**
- 日時が不明な「予定」の場合のみ  
  2025年12月（現在時刻）を基準に  
  常識的な時間（例：09:00〜12:00）を補完

**捏造の禁止**
- 画像にテキスト根拠がない情報は  
  `null` または空文字として出力する

---

### 4.2 編集・修正インターフェース

**リアクティブ・フォーム**
- 抽出された各項目を  
  即座に編集可能な入力フィールドとして表示

**アクション連動**
- ユーザーが1文字でも修正した場合  
  各アクションボタン（カレンダー、LINE等）の  
  リンクURLを即座に同期・更新する

---

### 4.3 外部アプリ連携（アクション）

**Googleカレンダー**
- 日本時間（JST）を厳守
- 日付形式：`YYYYMMDDTHHMMSS`
- 末尾の `Z` は除去する

**Googleマップ**
- 抽出または編集された「場所」情報を  
  クエリとして検索を実行

**LINE共有**
- 絵文字を活用した「黄金フォーマット」で整形
- メッセージ末尾に地図URLを付与（EVENT / PLACE時）

---

## 5. テクニカル・ガイドライン（制約事項）

### 5.1 開発スタック

- **Framework**: Next.js（App Router）
- **Styling**: Tailwind CSS
- **AI Engine**: Google Gemini（via Google AI Studio）

---

### 5.2 処理ロジックの優先順位

**タイムゾーン補正**
- 日本国内利用を前提とし  
  9時間ズレを防止する処理を最優先

**文字数制限**
- LINE共有時のエラー（HTTP 400）回避のため  
  メッセージ本文は最大800文字程度にトリミング

**UIの出し分け**
- カテゴリに応じて不要な入力フィールドは  
  DOMから完全に除去し、画面をスッキリさせる

